# Setup Steps

git token credential
add library untrusted
set environment aws accounts

cosign generate-key-pair --kms awskms:///alias/cosign

jfrog service account with api key
jfrog repos dev and prod
jfrog add project and setup projectkey
snyk setup
sonarqube projectkey and service account credential
