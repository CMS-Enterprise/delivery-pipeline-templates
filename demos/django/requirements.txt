# 4.2 does not support Python 3.14, which resources/pods/python.yaml provides:
# BaseContext.__copy__ calls copy(super()) and raises AttributeError. Fixed in 5.x.
django>=5.2,<6.0
gunicorn>=21.2.0
pylint-django>=2.7.0
