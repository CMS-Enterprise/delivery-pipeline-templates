[pytest]
DJANGO_SETTINGS_MODULE = mysite.settings
python_files = test_*.py
# pytestTest invokes `pytest tests/` from this directory, which does not put it on
# sys.path, so `mysite.settings` would not import without this.
pythonpath = .
